# Dedication, aligned right.
#
# Same block as dedicace-centre, with text-align: right. Format-neutral, hence
# no formats key.
label = "Dedication, aligned right"
section_type = text
css = css/templates/front-matter.css
options = {
	position = { type = select, values = [center, right], default = right }
}
