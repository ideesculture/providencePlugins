# Deliberately blank page.
#
# Carries no content: the section claims a page through the min-height of the
# stylesheet. Format-neutral, hence no formats key.
label = "Blank page"
section_type = text
css = css/templates/front-matter.css
